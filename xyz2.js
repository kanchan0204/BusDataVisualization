// xyz2.js

// Define a function to process the JSON data
function processJsonData(jsonData) {
    console.log("JSON data received in xyz2.js:", jsonData);
    // Here you can do further processing with the jsonData
}

export { processJsonData }; // Export the function to be accessible from other scripts
